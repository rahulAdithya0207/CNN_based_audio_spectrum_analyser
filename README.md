# CNN_based_audio_spectrum_analyser
Raw audio signals are converted into compact MFCC-based time-frequency features using pre-emphasis, framing, windowing, FFT, Mel filter banks, log compression, DCT, and normalization, after which a CNN classifies the audio into selected sound classes.
